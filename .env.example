# Copy this file to .env. Sign-in is done automatically in a browser window
# on first run if no valid session is found.

# --- Optional ---
# DEEPSEEK_PROFILE_DIR=session/profile        # reuse an existing signed-in Chrome profile
# DEEPSEEK_BROWSER_PATH=/usr/bin/google-chrome # custom browser executable path